# 记忆宫殿 Mind Palace

> 每次对话开始时请先读取 `_SYSTEM_RULES.md`，遵守所有规则后再执行任何操作。

---

## Vault 路径

```
YOUR_VAULT_PATH
```

---

## 启动检查（每次对话自动执行）

静默执行，输出非空时主动向用户展示：

```bash
python agents/learning_agent.py --action check_and_notify --vault "YOUR_VAULT_PATH"
```

---

## 门房技能（Portal Agent）

**触发词：** `@日记` / `记下来` / `记录下来` / `记一下` / `记录一下` / `记一嘴` / 消息包含"日记"

**核心约束：先判断内容归属**——"记下来"不等于门房，学习洞察走 learning_agent，日常生活/随想才走 portal_agent。

```bash
python agents/portal_agent.py --input "<内容>" --vault "YOUR_VAULT_PATH"
```

- Append-Only：严禁修改或删除 `_raw_inbox/` 中任何已写入内容

详见 [claude-code-skills/diary-skill/SKILL.md](claude-code-skills/diary-skill/SKILL.md)

---

## 学习技能（Learning Agent）

**触发词：** `/学习` / `@学习`

```bash
# 推荐项目
python agents/learning_agent.py --action recommend --vault "YOUR_VAULT_PATH"

# 加载项目
python agents/learning_agent.py --action load --project "<项目名>" --vault "YOUR_VAULT_PATH"
```

- 严禁直接用 Write/Edit 写入项目文件——必须通过 learning_agent.py 的对应 action

详见 [claude-code-skills/learning-skill/SKILL.md](claude-code-skills/learning-skill/SKILL.md)

---

## 复盘技能（Review Agent）

**触发词：** `/复盘` / `开始复盘` / `周复盘`

```bash
python agents/review_agent.py --action scan --vault "YOUR_VAULT_PATH"
```

- 严禁直接用 Write/Edit 写入 `_persona/`——必须通过 `update_persona` action
- `_persona/` 只有用户明确确认后才能写入

详见 [claude-code-skills/review-skill/SKILL.md](claude-code-skills/review-skill/SKILL.md)

---

## 炼金术技能（Alchemy Agent）

**触发词：** `/炼金` / `/批处理` / `运行炼金术`

```bash
python agents/alchemy_agent.py --action run --vault "YOUR_VAULT_PATH"
```

详见 [claude-code-skills/alchemy-skill/SKILL.md](claude-code-skills/alchemy-skill/SKILL.md)

---

## 目录权限速查

| 目录 | 写入权限 | 何时写入 |
|------|----------|----------|
| `_raw_inbox/` | Portal Agent | 实时 |
| `_private_sources/` | 仅用户手动 | 手动 |
| `日记/` | 炼金术 Agent | 凌晨 T-1 批处理 |
| `项目/` | 用户 + 学习 Agent | 主动发起 |
| `_compiled/` | 炼金术 Agent | 凌晨 T-1 批处理 |
| `_index.md` | 炼金术 Agent | 凌晨 T-1 批处理 |
| `_log/` | 学习 Agent + 炼金术 Agent | session 关闭时 + T-1 批处理 |
| `_social_graph/review/` | 战略复盘 Agent | 周末闭门会 |
| `_persona/` | 战略复盘 Agent | 周末闭门会 |
